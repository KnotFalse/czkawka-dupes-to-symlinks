#!/usr/bin/env bash
set -euo pipefail

if ! command -v cargo >/dev/null 2>&1; then
  echo "cargo is required for pre-commit checks" >&2
  exit 1
fi

echo "[pre-commit] running cargo fmt --all -- --check"
cargo fmt --all -- --check
